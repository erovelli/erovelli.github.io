---
import { pixelIconSource, type PixelIconName } from '../icons';

interface Props {
  name: PixelIconName;
  class?: string;
}

const { name, class: className } = Astro.props;
---

<span
  class:list={['pixel-icon', className]}
  style={pixelIconSource(name)}
  aria-hidden="true"></span>
